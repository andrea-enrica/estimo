{
  "name": "estimo-first-liquibase-lambda",
  "version": "1.0.0",
  "description": "Lambda function for ecr task(liquibase)",
  "main": "index.js",
  "scripts": {
    "build": "esbuild liquibase-lambda/index.ts --bundle --minify --platform=node --target=node20.10.0 --outdir=dist",
    "start": "node dist/index.js"  
  },
  "author": "Andrea Iordache",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-ecs": "^3.859.0"
  },
  "devDependencies": {
    "@types/aws-sdk": "^0.0.42",
    "@types/node": "^24.2.1",
    "esbuild": "^0.25.5",
    "typescript": "^5.1.6"
  }
}
